apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: inference
spec:
  type: NodePort
  selector:
    app: inference-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30801
